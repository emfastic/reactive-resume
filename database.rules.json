{ 
  "rules": {
    "users": {
      ".read": "false",
      ".write": "false",
        "$uid": {
          ".read": "$uid === auth.uid",
            ".write": "$uid === auth.uid"
        }
    }
  }
}